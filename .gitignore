# frontend: 소스 + 빌드 산출물(standalone, static)만 커밋. 개발용 node_modules 제외
apps/frontend/node_modules/
apps/frontend/.next/*
!apps/frontend/.next/standalone
!apps/frontend/.next/static

# api: EC2에서 npm 없이 바로 실행되도록 node_modules 를 의도적으로 커밋한다
# (제외 규칙 없음)

*.log
.DS_Store
